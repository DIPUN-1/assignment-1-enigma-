#include<iostream>
using namespace std;
int main () {
    int x;
    cin>>x;
    if(x > 0)
    cout<<"it is positive";
    else if(x == 0)
    cout<<"it is zero";
    else
    cout<<"it is negative";
    return 0;
}